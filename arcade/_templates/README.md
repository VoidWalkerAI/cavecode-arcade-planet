# Arcade Templates

This folder holds base templates for new CaveCode-powered games.

Use these when starting a new game in any subfolder (haze-maze, cavern-run, etc.):

- `cavecode-game-template.txt` – Base CaveCode spec (Blocks 1–5).
- (Optional) `index.html` – Minimal HTML shell wired for future JS logic.
